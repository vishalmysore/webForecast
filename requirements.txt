# Model definition + ONNX export (offline / one-time; CPU is fine for the 70M).
# torch is installed from the CPU wheel index:
#   pip install torch --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
safetensors>=0.4
onnx>=1.16
onnxruntime>=1.18      # for export-time checks + int8 dynamic quantization
numpy>=1.24

# The browser loads onnxruntime-web from a CDN (no Python dependency); see
# docs/timeSeriesWorker.js.
